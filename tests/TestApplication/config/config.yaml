imports:
    - { resource: "@SynoliaSyliusSchedulerCommandPlugin/config/config.yaml" }

# Uncomment if need to override entities with trait for example
#doctrine:
#    orm:
#        entity_managers:
#            default:
#                mappings:
#                    TestApplication:
#                        is_bundle: false
#                        type: attribute
#                        dir: '%kernel.project_dir%/../../../tests/TestApplication/src/Entity'
#                        prefix: Tests\Synolia\SyliusSchedulerCommandPlugin

sylius_fixtures:
    suites:
        default:
            listeners:
                orm_purger: ~
                logger: ~
            fixtures:
                scheduler_command:
                    options:
                        scheduled_commands:
                            -
                                name: 'Reset Sylius'
                                command: 'sylius:fixtures:load'
                                cronExpression: '0 0 * * *'
                                logFilePrefix: 'reset'
                                priority: 0
                                enabled: false
